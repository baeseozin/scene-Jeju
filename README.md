# scene-Jeju — 분위기 메이커

사용자의 현재 위치에서 제주 촬영 장소까지 도착하는 시각을 계산하고, 그 시각의 날씨·태양 위치·장소 특성·콘셉트를 합쳐 촬영 적합도를 `가능 / 보통 / 비추천`으로 안내하는 해커톤 MVP입니다.

기본값은 API 키 없이 실행되는 `mock` 모드입니다. 카카오 REST API 키가 있으면 제주 장소 검색과 자동차 실제 경로 조회를 시도하고, 기상청 키와 `APP_MODE=auto` 또는 `real`을 설정하면 실제 예보를 사용합니다. 외부 API 조회에 실패해도 `auto` 모드에서는 mock 또는 거리 기반 추정값으로 전환됩니다.

## 구현 기능

- 장소명 검색: 카카오 로컬 검색 결과를 선택하면 이름·좌표·장소 유형 자동 입력
- 지도 선택: 카카오 JavaScript 키가 있으면 실제 지도, 없으면 클릭 가능한 제주 간편 지도 제공
- 지도에서 지점을 누르면 주변 장소명·주소·좌표·장소 유형 자동 입력
- 장소 유형 수동 보정: 해변·숲길·도심·실내
- 콘셉트 3개: 청량한 여행·감성 필름·노을 실루엣
- 촬영 결과 선택: 사진 또는 15초 영상
- 이동수단 선택: 자동차·대중교통·도보
- 자동차는 카카오모빌리티 실제 경로 우선, 대중교통·도보는 거리 기반 예상 시간
- 출발 시각 + 이동시간으로 도착 예상 시각 계산
- 일반 콘셉트는 도착 직후부터 6시간 뒤, 노을 콘셉트는 12시간 뒤까지 1시간 간격으로 비교
- `가능 → 보통 → 비추천` 순으로 촬영 가능 여부를 먼저 비교하고, 같은 상태에서는 점수가 높고 빠른 시각 추천
- 추천 시각의 날씨·태양·점수·촬영 방향·가이드를 한 기준으로 표시
- 기상청 초단기예보와 단기예보 자동 전환
- 강수량·풍속·하늘상태(`맑음 / 구름 많음 / 흐림`) 반영
- `pvlib` 기반 태양 고도·방위각 계산
- 날씨 50% + 빛 35% + 장소 15%의 설명 가능한 점수
- 장소 유형과 콘셉트에 맞는 쉬운 촬영 위치 문장 제공
- 사진 구도 가이드와 15초 영상 촬영 가이드 분리
- 영상 클립 최대 4개를 브라우저 안에서 순서 변경·삭제 후 15초 세로 MP4 또는 WebM으로 자동 편집·저장
- 영상 원본 소리 유지, 사진 최대 4장은 9:16 JPG 콜라주로 저장
- 분석 기록 최대 10개를 브라우저에 저장
- 모바일 공유 시 분석 결과 이미지 카드를 첨부하고, 미지원 환경에서는 이미지 다운로드와 문구 복사로 대체
- 390px 모바일 화면 대응

## 빠른 실행

### 1. 환경 설정

프로젝트 루트에서 환경 파일을 만듭니다.

```bash
cp .env.example .env
```

키 없이 실행하려면 아래 상태로 둡니다.

```dotenv
APP_MODE=mock
KMA_SERVICE_KEY=
KAKAO_REST_API_KEY=
VITE_KAKAO_JAVASCRIPT_KEY=
FRONTEND_ORIGIN=http://localhost:5173
```

실제 데이터를 사용하려면 필요한 키를 입력합니다.

```dotenv
# 키가 있으면 실제 예보, 실패하면 mock으로 전환
APP_MODE=auto
KMA_SERVICE_KEY=공공데이터포털_일반인증키

# 장소 검색과 자동차 길찾기에 사용하는 서버용 REST API 키
KAKAO_REST_API_KEY=카카오디벨로퍼스_REST_API키

# 실제 카카오 지도 화면에 사용하는 프론트용 JavaScript 키
VITE_KAKAO_JAVASCRIPT_KEY=카카오디벨로퍼스_JavaScript키

FRONTEND_ORIGIN=http://localhost:5173
```

`KAKAO_REST_API_KEY`와 `VITE_KAKAO_JAVASCRIPT_KEY`는 서로 다른 키입니다. JavaScript 키를 쓰려면 카카오 디벨로퍼스에서 `http://localhost:5173`을 Web 플랫폼 도메인으로 등록해야 합니다. 키가 없으면 장소 검색은 사용할 수 없지만, 좌표 직접 입력과 간편 지도는 사용할 수 있습니다.

### 2. 백엔드

Python 3.11 이상이 필요합니다.

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

API 문서: [http://localhost:8000/docs](http://localhost:8000/docs)

### 3. 프론트엔드

새 터미널에서 실행합니다.

```bash
cd frontend
npm install
npm run dev
```

브라우저에서 [http://localhost:5173](http://localhost:5173)을 엽니다.

환경값을 변경하면 백엔드와 프론트 개발 서버를 모두 다시 시작해야 합니다.

## 데이터 사용 방식

### 이동시간

| 이동수단 | 계산 방식 | 결과 표시 |
| --- | --- | --- |
| 자동차 | REST 키가 있으면 카카오모빌리티 자동차 길찾기 API의 거리·소요시간 | 카카오 실제 자동차 경로 |
| 자동차 대체 경로 | API 권한·네트워크 오류 시 Haversine 거리, 제주 도로 우회율, 평균 속도 | 거리 기반 예상값 |
| 대중교통 | 거리, 평균 운행속도, 정류장 이동·배차 대기 15분 | 거리 기반 예상값 |
| 도보 | 직선거리, 보행 우회율, 평균 보행속도 4.5km/h | 거리 기반 예상값 |

카카오의 일반 공개 길찾기 API는 자동차용입니다. 카카오 도보 길찾기는 제휴 API이고 대중교통 공개 길찾기 API는 제공되지 않으므로, 이 MVP의 대중교통과 도보 시간은 실시간 경로가 아닙니다. 결과 화면에서도 실제값과 예상값을 구분합니다.

### 날씨

- 도착 시각이 초단기예보 범위 안이면 `getUltraSrtFcst`의 `RN1`, `WSD`, `SKY`를 사용합니다.
- 범위를 벗어나면 `getVilageFcst`의 `PCP`, `WSD`, `SKY`를 사용합니다.
- `APP_MODE=mock`: 항상 결정론적 데모 날씨
- `APP_MODE=auto`: 실제 예보 우선, 실패하면 mock
- `APP_MODE=real`: 실제 예보 실패 시 API 오류 반환
- 화면의 `LIVE WEATHER / DEMO WEATHER` 표시는 실제 분석 응답의 데이터 출처를 기준으로 바뀝니다.

정확한 구름 퍼센트는 계산하지 않습니다. 기상청 `SKY` 값에 맞춰 `맑음 / 구름 많음 / 흐림`만 사용합니다.

### 태양과 촬영 방향

태양 고도와 방위각은 timezone이 포함된 도착 예상 시각으로 `pvlib`에서 계산합니다. 청량한 여행은 순광, 감성 필름은 측광, 노을 실루엣은 역광 관계가 되도록 촬영자가 설 위치를 계산합니다. 화면에는 각도보다 다음과 같은 행동 문장을 먼저 보여줍니다.

> 바다가 인물 뒤에 넓게 보이도록 자리를 잡고, 촬영자는 태양을 등진 채 인물을 바라보세요.

실내 장소는 비·강풍·일몰 때문에 자동으로 비추천되지 않으며, 창문과 밝은 조명 배치를 중심으로 평가·안내합니다.

## 점수 산정

1. 강수 45% + 풍속 25% + 하늘상태 30%로 날씨 점수를 계산합니다.
2. 태양 고도와 콘셉트별 광원 관계로 빛 점수를 계산합니다.
3. 장소 유형과 콘셉트의 적합성을 장소 점수로 계산합니다.
4. `날씨 50% + 빛 35% + 장소 15%`로 총점을 계산합니다.

| 총점 | 판정 |
| --- | --- |
| 75점 이상 | 가능 |
| 50~74점 | 보통 |
| 49점 이하 | 비추천 |

야외에서 태양 고도가 `-6°` 미만이거나 강수량이 `5mm` 이상이면 안전장치로 비추천 처리합니다. 실내에는 이 강제 규칙을 적용하지 않습니다.

## 사진·영상 편집과 저장

- 영상 파일은 서버로 전송하지 않고 사용자의 브라우저 안에서만 처리합니다.
- 파일 하나는 150MB, 선택한 파일 전체는 300MB까지 받습니다.
- 최대 4개의 순서를 바꾸거나 불필요한 파일을 제거할 수 있습니다.
- 영상을 중앙 크롭하여 9:16 비율, 원본 소리를 포함한 15초 결과로 내보냅니다.
- Safari처럼 MP4 녹화를 지원하는 브라우저는 MP4, Chrome 계열은 WebM으로 저장합니다.
- 실제 영상 편집에 약 15초가 걸리며 브라우저가 지원하는 코덱에 따라 저장 형식이 달라집니다.
- 사진 모드에서는 최대 4장을 1080×1920 JPG 콜라주로 저장합니다.
- 분석 기록은 서버 DB가 아니라 브라우저 `localStorage`에 최대 10개 저장됩니다.
- 공유는 결과 점수·추천 시각·날씨·촬영 방향·촬영 순서가 담긴 PNG 카드를 만듭니다. Web Share API 파일 공유를 우선 사용하고, 미지원 환경에서는 이미지를 다운로드하고 문구를 복사합니다.

## API

- `GET /api/health`: 서버 상태
- `GET /api/catalog`: 예시 장소와 콘셉트
- `GET /api/places/search?query=함덕해수욕장`: 제주 장소 검색
- `GET /api/places/reverse?latitude=33.5431&longitude=126.6692`: 지도 좌표 주변 장소 확인
- `POST /api/analyze`: 이동시간, 시간대별 날씨·빛, 최적 시각, 촬영 가이드 분석

요청 예시:

```bash
curl -X POST http://localhost:8000/api/analyze \
  -H 'Content-Type: application/json' \
  -d '{
    "current_location": {"latitude": 33.5104, "longitude": 126.4914},
    "place": {
      "name": "함덕해수욕장",
      "latitude": 33.5431,
      "longitude": 126.6692,
      "place_type": "beach"
    },
    "concept_id": "refreshing",
    "capture_mode": "video",
    "transport_mode": "car"
  }'
```

## 테스트

```bash
cd backend
python -m pytest -q

cd ../frontend
npm run build
```

현재 자동 테스트는 기상청 격자 변환, 촬영 점수, 상태 우선 추천, 실내 판정, 이동수단별 시간, 촬영 방향, 장소 유형 추론, 지도 역검색, 제주 좌표 검증, 예시 장소 전용 가이드, 사진·대중교통 옵션을 포함합니다.

## 파일 구조

```text
.
├── .env.example
├── README.md
├── backend
│   ├── app
│   │   ├── main.py
│   │   ├── config.py
│   │   ├── data.py
│   │   ├── models.py
│   │   └── services
│   │       ├── travel.py
│   │       ├── place_search.py
│   │       ├── weather.py
│   │       ├── solar.py
│   │       └── scoring.py
│   └── tests
└── frontend
    └── src
        ├── App.tsx
        ├── KakaoMapPicker.tsx
        ├── MediaStudio.tsx
        ├── share.ts
        ├── api.ts
        ├── types.ts
        └── styles.css
```

## 사실대로 말해야 하는 제한

- 대중교통과 도보는 실제 경로·배차 정보가 아닌 예상값입니다.
- `SKY`는 정확한 구름 퍼센트가 아니라 3단계 하늘상태입니다.
- 장소 유형은 카카오 장소명과 카테고리의 키워드로 추론하므로 사용자가 수정할 수 있게 했습니다.
- 임의 장소의 촬영법은 장소 유형과 콘셉트별 템플릿입니다. 현장 장애물이나 건물 방향을 카메라로 인식한 결과는 아닙니다.
- 자동 편집은 장면 의미를 AI가 이해하는 편집이 아니라, 선택한 클립을 순서대로 길이·비율에 맞추는 클라이언트 편집입니다.
- 영상의 MP4/WebM 형식은 브라우저의 `MediaRecorder` 지원 코덱에 따라 달라집니다.
- 실제 기상청 데이터는 별도 서비스 키가 있어야 검증할 수 있습니다.
